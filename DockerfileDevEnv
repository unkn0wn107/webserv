FROM debian:bullseye-slim
RUN apt-get update && apt-get install -y make clang python3 php-cgi libpthread-stubs0-dev
RUN mkdir -p /var/www/html

WORKDIR /app

COPY ./build.sh .

ENTRYPOINT [ "./build.sh" ]
CMD ["tail", "-f", "/dev/null"]
