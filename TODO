- TIMEOUT: alex
- errno on accept: mat
- You can use every macro and define like FD_SET, FD_CLR, FD_ISSET, FD_ZERO (understanding what and how they do it is very useful).
- test cross sieges (multiple socket): mat
- Because you won’t call the CGI directly, use the full path as PATH_INFO: alex
- Just remember that, for chunked request, your server needs to unchunk it, the CGI will expect EOF as end of the body: (on verra)
- Same things for the output of the CGI. If no content_length is returned from the CGI, EOF will mark the end of the returned data: mat
- The CGI should be run in the correct directory for relative path file access: alex
